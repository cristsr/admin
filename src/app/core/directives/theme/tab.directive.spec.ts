import { TabDirective } from './tab.directive';

describe('TabDirective', () => {
  it('should create an instance', () => {
    const directive = new TabDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
