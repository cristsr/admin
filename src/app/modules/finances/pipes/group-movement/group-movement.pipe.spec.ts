import { GroupMovementPipe } from './group-movement.pipe';

describe('GroupMovementPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupMovementPipe();
    expect(pipe).toBeTruthy();
  });
});
